
# example with League of Legends' api

events:
  # vibrate for 5 seconds at 50 intensity only if it's an enemy tower that is destroyed
  turret_destroy:
    intensity: 50
    duration: 5
    condition: "value == \"enemy\""

  # vibrate for 0.2 seconds when dealing damage but intensity range from 1 to 50 based off damage from 10- to 500+ 
  physical_damage_dealt_to_champions:
    intensity: 10
    duration: 0.2
    intensity_modifier: [
      {
        intensity: "intensity * value / 100.0",
        intensity_min: 1,
        intensity_max: 50
      }
    ]
  
  # vibrate for different values depending on the killing streak
  kill:
    intensity: 50
    duration: 5
    intensity_modifier: [
      {
        condition: "value.label == \"penta_kill\"",
        intensity: 100,
        duration: 20,
        break: true
      },
      {
        condition: "value.label == \"quadra_kill\"",
        intensity: 85,
        duration: 10,
        break: true
      },
      {
        condition: "value.label == \"triple_kill\"",
        intensity: 70,
        duration: 10,
        break: true
      },
      {
        condition: "value.label == \"double_kill\"",
        duration: 10,
        break: true
      }
    ]
    
